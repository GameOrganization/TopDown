#include "camera.h"

camera::camera(){
    x = 0.0f;
    y = 0.0f;
}

camera::~camera(){}

void camera::moveCamera(float mouseX, float mouseY, float deltaT, int key, int scancode, int action, int mods){

}
